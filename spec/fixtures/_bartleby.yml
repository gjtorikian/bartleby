config_variables:
  secret_name: "clever"

data_variables:
  -
    scope:
      path: ""
    values:
      version: "dotcom"

page_variables:
  -
    scope:
      path: ""
    values:
      version: "dotcom"
      secret_name: "The name is %{secret_name}"
  -
    scope:
      path: "different"
    values:
      version: "2.0"
